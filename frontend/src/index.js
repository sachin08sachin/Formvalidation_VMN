import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';

const container = document.getElementById('root');

const rooty = ReactDOM.createRoot(container);
rooty.render(
<React.StrictMode>
  <App />
</React.StrictMode>

);


